# CLAUDE.md

Read `AGENTS.md` first — it is the single source of truth for this
repository (project purpose, structure, commands, rules, priorities). This
file only adds Claude-specific notes.

- Do not rely on, or refer to, any previous Claude conversation. Everything
  needed to continue this project is in `AGENTS.md`, `docs/`, and the code
  itself. If something important was decided but isn't written down
  anywhere in the repo, write it down before finishing your session
  (`docs/DECISIONS.md` or an ADR).
- Document consequential decisions as you make them, not from memory
  afterward.
- Run `pnpm validate` (or at least lint + typecheck + test) before
  reporting a task complete.
